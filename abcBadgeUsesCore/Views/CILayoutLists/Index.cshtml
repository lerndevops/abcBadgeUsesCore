@model ciBadgeForWeb.Models.ViewModels.CILayoutLists

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<div>
    <h4>CILayoutLists</h4>
    <hr />
    <dl class="dl-horizontal"></dl>
</div>


@foreach (var each_item in Model.LayoutTextFields)
{
    Layout = "CILayoutText.cshtml";
}

@foreach (var each_item in Model.LayoutPictures)
{
    Layout = "CILayoutPicture.cshtml";
}


<p>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    @Html.ActionLink("Back to List", "Index")
</p>

